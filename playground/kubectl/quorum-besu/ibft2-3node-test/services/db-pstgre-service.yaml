apiVersion: v1
kind: Service
metadata:
  name: db-postgre
  labels:
    app: db-postgre
  namespace: blockscout
spec:
  type: NodePort
  selector:
    app: postgre
  ports:
    - port: 5432
      targetPort: 5432
      nodePort: 31000
      protocol: TCP
      name: db-postgre

