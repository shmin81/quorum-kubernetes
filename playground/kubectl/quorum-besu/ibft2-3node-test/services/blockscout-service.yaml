apiVersion: v1
kind: Service
metadata:
  name: blockscout
  labels:
    app: blockscout
  namespace: blockscout
spec:
  type: NodePort
  selector:
    app: blockscout
  ports:
    - port: 4000
      targetPort: 4000
      nodePort: 31400
      protocol: TCP
      name: blockscout
