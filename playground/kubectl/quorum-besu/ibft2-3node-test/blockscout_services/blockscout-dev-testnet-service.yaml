
apiVersion: v1
kind: Service
metadata:
  name: blockscout-dev-testnet
  labels:
    app: blockscout-dev-testnet
  namespace: blockscout
spec:
  type: NodePort
  selector:
    app: blockscout-dev-testnet
  ports:
    - port: 4000
      targetPort: 4000
      nodePort: 31401
      protocol: TCP
      name: blockscout-dev-test
